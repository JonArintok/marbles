
hyaku num = 100

addmulV
	input  num
	factor num
	num =
	mul factor (add factor input)

addmulH (input num) (factor num) num = mul factor (add factor input)


# enter

V num = addmulV hyaku currentFrame

H num = addmulH hyaku currentFrame
