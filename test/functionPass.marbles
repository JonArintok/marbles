
juu num = 10

addN_mulN
	input  num
	factor num
	num =
	mul factor (add factor input)
	
mulN_addN
	input  num
	factor num
	num =
	add factor (mul factor input)


maxDoer
	doerA num num > num
	doerB num num > num
	x num
	y num
	num num > num =
	if (gt (doerA x y) (doerB x y)) doerA doerB

minDoer
	doerA num num > num
	doerB num num > num
	x num
	y num
	num num > num =
	if (lt (doerA x y) (doerB x y)) doerA doerB


doerSelectorSelector
	doerSelectorA (num num > num) (num num > num) > (num num > num)
	doerSelectorb (num num > num) (num num > num) > (num num > num)
	(num num > num) (num num > num) > (num num > num) =
	if (mod currentFrame 2) doerSelectorA doerSelectorb

	
# main

currentDoerSelector
	(num num > num) (num num > num) > (num num > num) =
	doerSelectorSelector maxDoer minDoer

currentDoer
	num num > num =
	currentDoerSelector addN_mulN mulN_addN juu 10

outA num = currentDoer juu 10

outB num = currentDoer 10 juu

